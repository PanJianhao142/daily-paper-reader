---
title: Temporal Consistency-Aware Text-to-Motion Generation
title_zh: 时序一致性感知的文本到动作生成
authors: "Hongsong Wang, Wenjing Yan, Qiuxia Lai, Xin Geng"
date: 2026-02-20
pdf: "https://arxiv.org/pdf/2602.18057v1"
tags: ["keyword:MDM", "query:课题"]
score: 8.0
evidence: 解决人体运动生成中的长期时间一致性问题
tldr: 文本到动作生成（T2M）旨在根据自然语言合成人体动作。针对现有两阶段框架忽视跨序列时间一致性而导致的语义偏差和物理不合理问题，本文提出TCA-T2M框架。该框架引入了时间一致性感知空间VQ-VAE（TCaS-VQ-VAE）以实现跨序列对齐，并结合掩码动作Transformer进行生成，同时利用运动学约束块减少离散化伪影。实验证明该方法在HumanML3D和KIT-ML基准上达到了SOTA水平，显著提升了动作生成的鲁棒性与连贯性。
motivation: 现有方法忽视了同类动作间的跨序列时间一致性，导致生成的动作存在语义对齐差和物理不合理的问题。
method: 提出TCA-T2M框架，包含时间一致性感知空间VQ-VAE、掩码动作Transformer以及用于减少伪影的运动学约束块。
result: 在HumanML3D和KIT-ML主流基准测试中，该方法均取得了当前最先进的性能表现。
conclusion: 引入时间一致性感知机制对于生成稳健、连贯且符合物理规律的人体动作具有至关重要的作用。
---

## 摘要
文本到动作（T2M）生成旨在根据自然语言描述合成逼真的人体动作序列。虽然利用离散动作表示的两阶段框架推动了 T2M 研究的发展，但它们往往忽略了跨序列的时序一致性，即同一动作的不同实例之间存在的共享时序结构。这导致了语义对齐偏差和物理上不合理的动作。为了解决这一局限性，我们提出了 TCA-T2M，这是一个用于时序一致性感知 T2M 生成的框架。我们的方法引入了时序一致性感知空间 VQ-VAE (TCaS-VQ-VAE) 以实现跨序列时序对齐，并结合掩码动作 Transformer 进行文本条件下的动作生成。此外，运动学约束模块减轻了离散化伪影，以确保物理合理性。在 HumanML3D 和 KIT-ML 基准测试上的实验表明，TCA-T2M 达到了最先进的性能，突显了时序一致性在鲁棒且连贯的 T2M 生成中的重要性。

## Abstract
Text-to-Motion (T2M) generation aims to synthesize realistic human motion sequences from natural language descriptions. While two-stage frameworks leveraging discrete motion representations have advanced T2M research, they often neglect cross-sequence temporal consistency, i.e., the shared temporal structures present across different instances of the same action. This leads to semantic misalignments and physically implausible motions. To address this limitation, we propose TCA-T2M, a framework for temporal consistency-aware T2M generation. Our approach introduces a temporal consistency-aware spatial VQ-VAE (TCaS-VQ-VAE) for cross-sequence temporal alignment, coupled with a masked motion transformer for text-conditioned motion generation. Additionally, a kinematic constraint block mitigates discretization artifacts to ensure physical plausibility. Experiments on HumanML3D and KIT-ML benchmarks demonstrate that TCA-T2M achieves state-of-the-art performance, highlighting the importance of temporal consistency in robust and coherent T2M generation.

---

## 论文详细总结（自动生成）

这是一份关于论文《Temporal Consistency-Aware Text-to-Motion Generation》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的“文本到动作”（T2M）生成框架（尤其是基于 VQ-VAE 的两阶段模型）主要关注单体动作的重构，却忽视了**跨序列的时序一致性**（Cross-sequence temporal consistency）。
*   **研究背景**：同一类动作（如“行走”）在不同实例中应具有共享的时序结构（如起步、落脚的时机）。现有模型由于缺乏这种跨序列的约束，往往导致生成的动作与文本语义对齐不准，且容易出现物理上的不合理现象（如“滑步”/Leg Sliding）。
*   **整体含义**：本文旨在通过引入时序一致性感知机制，使模型能够捕捉同类动作间的共同演化规律，从而提升生成动作的语义保真度和物理真实感。

### 2. 方法论：核心思想与关键技术
论文提出了 **TCA-T2M** 框架，其核心由三个部分组成：
*   **TCaS-VQ-VAE（时序一致性感知空间 VQ-VAE）**：
    *   **核心思想**：利用**循环一致性学习（Cycle-consistent learning）**。在训练离散表示空间时，强制要求同一动作类别的不同序列在潜在空间中能够实现时序上的相互映射。
    *   **关键技术**：
        *   **循环时序一致性损失 ($L_{tcc}$)**：包含分类损失（验证映射的闭环性）和回归损失（优化时序对齐的精确度，采用 MSE 衡量）。
        *   **残差量化（Residual Quantization）**：通过多层量化减少离散化带来的信息损失。
*   **运动学约束块（Kinematic Constraint Block, KCB）**：
    *   在解码器输出端引入物理约束，通过计算关节速度、足部接触标签并结合交叉注意力机制，修正根节点旋转误差，显著减少滑步现象。
*   **掩码动作 Transformer（Masked Motion Transformer）**：
    *   采用双 Transformer 结构：**Motion Transformer** 负责生成基础动作 Token，**Residual Transformer** 负责分层预测残差 Token，实现从粗到精的文本-动作对齐生成。

### 3. 实验设计
*   **数据集**：
    *   **HumanML3D**：目前规模最大的 3D 人体动作-语言数据集。
    *   **KIT-ML**：经典的动作描述基准数据集。
*   **Benchmark（评估指标）**：
    *   **FID**（生成质量）、**R-Precision**（文本-动作匹配度）、**MM-Dist**（多模态距离）、**Diversity**（多样性）和 **MModality**（多模态多样性）。
*   **对比方法**：
    *   对比了包括 T2M-GPT、MDM、MLD、Momask、MMM、MotionGPT 等在内的十余种近年来最先进的（SOTA）模型。

### 4. 资源与算力
*   **硬件环境**：实验使用了 **NVIDIA RTX 4090 GPU** 进行训练和推理。
*   **训练细节**：
    *   RVQ-VAE 训练的 Mini-batch size 为 256。
    *   Transformer 训练在 HumanML3D 上为 64，在 KIT-ML 上为 32。
*   **效率说明**：引入 TCC 约束后，单次迭代训练时间从 0.06s 增加到 0.26s，但由于模型参数量未变，**推理时间不受影响**（单样本推理约 0.09s）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **定量对比**：在两个主流数据集上进行了全面的性能排名。
    *   **消融实验**：针对 TCC 损失函数类型（分类 vs MSE vs Huber）、循环长度（L=2 到 5）、Codebook 大小（512 到 4096）进行了详尽测试。
    *   **定性分析**：展示了复杂指令下的动作对比、长序列生成（Long-sequence）以及零样本生成（Zero-shot）的可视化结果。
*   **充分性评价**：实验设计非常充分，不仅验证了生成质量，还专门通过 Kendall’s tau 指标量化了时序一致性的提升，客观地证明了所提组件的有效性。

### 6. 主要结论与发现
*   **性能领先**：TCA-T2M 在 HumanML3D 和 KIT-ML 的 FID 和 R-Precision 指标上均达到了 SOTA 水平。
*   **时序一致性的价值**：引入 TCC 约束能显著提升动作的逻辑连贯性，使模型在处理“行走中转身”等复合动作时表现更稳健。
*   **物理真实性**：KCB 模块有效解决了离散 Token 导致的动作不连续问题，生成的动作在视觉上更符合生物力学。

### 7. 优点与亮点
*   **创新视角**：首次将跨序列的循环一致性引入到 T2M 的离散表示学习中，解决了长期被忽视的时序结构共享问题。
*   **质量与效率平衡**：在提升生成精度的同时，保持了极高的推理效率，适合实时应用。
*   **泛化能力**：展示了优秀的零样本生成能力，能够处理训练集中未出现的动作组合。

### 8. 不足与局限
*   **语义理解瓶颈**：在处理包含多个连续、剧烈动作变化的复杂文本时（如“倒立后翻滚再跳跃”），模型仍可能出现语义对齐失败或姿态扭曲。
*   **数据依赖**：受限于现有数据集的规模，生成的动作多样性仍有提升空间。
*   **长序列挑战**：虽然支持长序列生成，但在实时生成超长且逻辑复杂的动作流时，计算开销和逻辑一致性仍是挑战。

（完）
