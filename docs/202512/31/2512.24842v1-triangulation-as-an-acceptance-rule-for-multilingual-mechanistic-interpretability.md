
# Triangulation as an Acceptance Rule for Multilingual Mechanistic Interpretability

# 三角定位作为多语言机制可解释性的接受规则



**Authors**: Yanan Long

**Date**: 2025-12-31

**Tags**: <span class="tag-label tag-green">keywords: symbolic regression</span> <span class="tag-label tag-green">keywords: transformer</span>

---

## Abstract
Multilingual language models achieve strong aggregate performance yet often behave unpredictably across languages, scripts, and cultures. We argue that mechanistic explanations for such models should satisfy a \emph{causal} standard: claims must survive causal interventions and must \emph{cross-reference} across environments that perturb surface form while preserving meaning. We formalize \emph{reference families} as predicate-preserving variants and introduce \emph{triangulation}, an acceptance rule requiring necessity (ablating the circuit degrades the target behavior), sufficiency (patching activations transfers the behavior), and invariance (both effects remain directionally stable and of sufficient magnitude across the reference family). To supply candidate subgraphs, we adopt automatic circuit discovery and \emph{accept or reject} those candidates by triangulation. We ground triangulation in causal abstraction by casting it as an approximate transformation score over a distribution of interchange interventions, connect it to the pragmatic interpretability agenda, and present a comparative experimental protocol across multiple model families, language pairs, and tasks. Triangulation provides a falsifiable standard for mechanistic claims that filters spurious circuits passing single-environment tests but failing cross-lingual invariance.



## 摘要
多语言语言模型虽然取得了强大的总体性能，但在不同语言、文字和文化之间往往表现不可预测。我们认为，此类模型的机制解释应满足一种因果标准：相关主张必须经得起因果干预的检验，且必须在保持含义的同时扰动表层形式的不同环境之间进行交叉参照。我们将“参照族”形式化为保持谓词的变体，并引入“三角定位”，这是一种接受规则，要求满足必要性（消融电路会降低目标行为）、充分性（修补激活值可转移该行为）以及不变性（上述两种效应在整个参照族中均保持方向稳定且幅度足够大）。为了提供候选子图，我们采用自动电路发现技术，并通过三角定位对这些候选子图进行接受或拒绝判定。我们通过将三角定位表述为在交换干预分布上的近似变换得分，将其建立在因果抽象理论之上，将其与实用主义可解释性议程联系起来，并提出了涵盖多个模型族、语言对和任务的比较实验方案。三角定位为机制主张提供了一种可证伪的标准，能够过滤掉那些通过单环境测试但无法满足跨语言不变性的虚假电路。


---

## 论文详细总结（自动生成）

### 论文总结：三角定位作为多语言机制可解释性的接受规则

#### 1. 论文的核心问题与整体含义
*   **研究背景**：多语言语言模型虽然在总体性能上表现强劲，但在不同语言、文字和文化环境下往往表现出不可预测的不稳定性。现有的机制可解释性研究多关注单一环境下的关联性，难以区分真正的因果机制与仅依赖特定语言表面特征（如语言 ID、脚本、标点符号）的虚假捷径。
*   **核心问题**：如何建立一个严格的因果标准，用于验证多语言模型内部的机制解释是否真正有效，而不仅仅是由于特定环境的表面相关性所导致的巧合。
*   **整体含义**：论文提出了一种名为“三角定位”的接受规则，旨在通过因果干预和跨环境交叉验证，筛选出真正鲁棒、跨语言通用的机制，从而提升对多语言模型内部工作原理理解的可信度。

#### 2. 论文提出的方法论
*   **核心思想**：将机制解释视为一个必须通过因果测试的假设。引入“参照族”概念，即保持语义谓词不变但改变表面形式（如语言、风格）的输入变体集合。一个机制只有同时满足必要性、充分性和不变性，才能被接受。
*   **关键技术细节**：
    *   **参照族**：构造一组输入 $R(x) = \{r_1, ..., r_K\}$，使得 $\pi(r_k) = \pi(x)$（谓词 $\pi$ 不变），但环境属性（语言、脚本等）变化。
    *   **机制类**：考虑到不同环境下实现同一抽象计算的底层电路可能不同，测试对象为环境索引的电路族 $\{C_e\}_{e \in E}$ 及其对应的翻译映射。
    *   **翻译修补**：针对跨环境修补可能出现的流形不匹配或类型不兼容问题，使用学习到的映射 $T_{e_b \leftarrow e_s}$ 将源激活值映射到目标环境的坐标系中。
*   **三大接受标准（三角定位准则）**：
    1.  **必要性**：对电路进行消融，必须导致目标行为显著下降（$\Delta_{KO} \ge \tau_N$）。
    2.  **充分性**：通过谓词交换（Predicate-swap）进行激活修补，必须能成功转移行为（改变方向和幅度符合预期，$\Delta_{swap}$）。
    3.  **不变性**：必要性和充分性必须在参照族的所有环境中保持方向稳定且幅度足够；同时，针对仅包含表面线索的电路进行负控制测试，应无法产生显著效果。
*   **算法与公式**：
    *   将三角定位定义为因果抽象中的近似变换得分：
        $$T_{tri} := \mathbb{E}_{I \sim P} [\text{Sim}(\tau(\text{Solve}(L_I)), \tau(\text{Solve}(H_{\omega(I)})))]$$
    *   其中 $L$ 为底层模型，$H$ 为高层抽象模型，$P$ 为干预分布（包含 KO、修补、稳定性测试等），$\text{Sim}$ 为相似度函数。
    *   使用 Beta-二项分布模型对得分进行贝叶斯估计和不确定性量化。

#### 3. 实验设计
*   **数据集 / 场景**：
    *   **FairTranslate**：英语-法语翻译，包含性别标记变体。
    *   **GLITTER**：英语-德语翻译，包含多种性别公平策略。
    *   **mGeNTE**：基于 Europarl 的多语言（英、意、西、德、希腊）数据集。
    *   **任务类型**：固定目标语言的翻译（关注性别标记位置的 Logit 边际）、语内重写（关注中性化分类）。
*   **模型**：涵盖多个模型家族以验证泛化性，包括 Gemma 3 (1B, 4B)、Llama 3.2 (1B, 3B)、Mistral 3B 以及 MADLAD-400-3B-MT（专门的机器翻译模型）。
*   **对比方法**：
    1.  单环境修补。
    2.  因果中介分析。
    3.  因果擦除。
    4.  基于消融的电路测试（如 ACDC）。
*   **Benchmark**：使用上述数据集构建比较实验协议，重点评估不同方法对“虚假电路”的过滤能力及对“真实电路”的保留能力。

#### 4. 资源与算力
*   **算力情况**：虽然论文在贡献中提到了“显式算力核算”，但在提供的文本内容中**并未列出具体的 GPU 型号、数量、训练时长或具体的浮点运算数**。论文主要描述了实验协议和方案设计，而非具体的实验执行日志。

#### 5. 实验数量与充分性
*   **实验覆盖**：
    *   涵盖了 4 个主要模型家族。
    *   涉及多种语言对（英-法、英-德、多语言）和书写系统。
    *   包含了翻译和重写两种不同任务类型。
    *   包含了详细的工作案例研究（FairTranslate），展示了从边缘归因到三角定位评估的全流程。
*   **充分性与客观性**：
    *   **充分性**：设计较为全面，不仅有正向测试（必要性、充分性），还有负向控制测试（仅表面线索干扰）和压力测试（故意引入谓词违背）。同时考虑了单环境与跨环境的差异。
    *   **客观性**：方法论强调预注册的接受阈值和针对安慰剂电路的校准，试图减少人为偏差。分离了“发现”与“接受”阶段，避免了针对特定结果的手动调整。

#### 6. 论文的主要结论与发现
*   **有效性**：三角定位能够提供一个可证伪的标准，有效过滤掉那些在单环境下看似有效但在跨语言环境下失效的虚假电路。
*   **机制区分**：该方法能够明确区分“视觉/语言无关机制”（跨环境稳定）、“语言/线索机制”（单环境成功但跨环境失败）和“虚假机制”。
*   **多模态扩展**：框架可以自然扩展到视觉-语言模型（VLM），通过固定图像改变提示语言，来分离视觉处理机制与语言相关性机制。
*   **理论贡献**：成功将机制可解释性中的经验实践与因果抽象理论及不变性因果预测原理相结合。

#### 7. 优点
*   **理论严谨**：将解释标准建立在因果抽象的结构化因果模型（SCM）之上，而非启发式的经验法则。
*   **鲁棒性高**：通过“不变性”要求，直接针对多语言模型的痛点（跨语言不稳定性）设计，有效防止了模型利用特定语言的表面捷径。
*   **通用性强**：不仅限于文本任务，文中展示了该方法在多模态场景下的直接适用性，适用于任何存在“保持谓词不变的环境变化”的场景。
*   **评估清晰**：通过 Beta-二项分布后验估计提供了明确的不确定性量化，使得接受/拒绝决策具有统计学意义。

#### 8. 不足与局限
*   **非唯一性**：论文承认三角定位可能识别出多个冗余的机制（即多个电路都能满足标准），因此不保证机制的唯一性。
*   **数据依赖**：方法的有效性高度依赖于“参照族”的质量。如果参照族未能严格保持谓词不变，或变体之间差异过大，可能导致误判（接受虚假电路或拒绝真实电路）。
*   **计算成本**：大规模的因果干预测试（包括跨环境修补和校准）计算开销巨大，可能限制在大模型上的广泛应用。
*   **循环性风险**：在使用 LLM 作为裁判验证谓词保持时，如果裁判模型与被测模型高度相关，可能会引入循环偏差。