---
title: "SimpliHuMoN: Simplifying Human Motion Prediction"
title_zh: "SimpliHuMoN: 简化人体运动预测"
authors: "Aadya Agrawal, Alexander Schwing"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.04399v1"
tags: ["query:课题"]
score: 8.0
evidence: 直接针对人体运动预测，使用时空 Transformer 处理姿态和轨迹。
tldr: 本研究针对人体运动预测中轨迹预测与姿态预测难以有效结合的问题，提出了SimpliHuMoN模型。该模型采用简洁的Transformer架构，通过堆叠自注意力机制同时捕捉空间和时间依赖关系。作为一个通用的端到端模型，它无需针对特定任务进行修改即可处理姿态、轨迹及两者的结合预测，在Human3.6M、AMASS等多个基准数据集上均达到了SOTA水平。
motivation: 现有的专门化模型在整合轨迹与姿态预测时效果不佳，且难以在单一任务基准上保持竞争力。
method: 提出一种基于Transformer的端到端模型，利用堆叠的自注意力模块捕捉人体姿态的空间依赖和运动序列的时间关系。
result: 在Human3.6M、AMASS、ETH-UCY和3DPW等多个主流数据集上，该方法在所有预测任务中均取得了最先进的性能。
conclusion: 这种简化且通用的架构证明了无需复杂的任务特定设计，也能在人体运动预测领域实现卓越的性能。
---

## 摘要
人体运动预测结合了轨迹预测和人体姿态预测任务。针对这两个任务，目前已开发出专门的模型。然而，将这些模型结合以实现整体人体运动预测并非易事，且近期的模型在单一任务的既定基准测试中难以保持竞争力。为解决这一问题，我们提出了一种简单而有效的基于 Transformer 的人体运动预测模型。该模型采用堆叠的自注意力模块，以有效捕捉姿态内部的空间依赖关系以及运动序列中的时间关系。这种简单、精简的端到端模型具有足够的通用性，无需针对特定任务进行修改即可处理仅姿态、仅轨迹以及组合预测任务。通过在 Human3.6M、AMASS、ETH-UCY 和 3DPW 等广泛的基准数据集上进行的详尽实验，我们证明了该方法在所有任务中均达到了最先进（SOTA）的性能。

## Abstract
Human motion prediction combines the tasks of trajectory forecasting and human pose prediction. For each of the two tasks, specialized models have been developed. Combining these models for holistic human motion prediction is non-trivial, and recent methods have struggled to compete on established benchmarks for individual tasks. To address this, we propose a simple yet effective transformer-based model for human motion prediction. The model employs a stack of self-attention modules to effectively capture both spatial dependencies within a pose and temporal relationships across a motion sequence. This simple, streamlined, end-to-end model is sufficiently versatile to handle pose-only, trajectory-only, and combined prediction tasks without task-specific modifications. We demonstrate that this approach achieves state-of-the-art results across all tasks through extensive experiments on a wide range of benchmark datasets, including Human3.6M, AMASS, ETH-UCY, and 3DPW.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **SimpliHuMoN** 的人体运动预测模型，旨在通过极简的架构统一处理轨迹预测和姿态预测任务。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：人体运动预测通常被拆分为“轨迹预测”（重心移动）和“姿态预测”（肢体动作）两个独立任务。虽然各自领域已有成熟模型，但将二者结合的整体模型往往在单一任务的基准测试（Benchmark）中表现不佳。
*   **核心挑战**：现有的整体模型通常依赖复杂的多阶段流水线或特定领域的先验知识（如离散余弦变换 DCT 或图卷积网络 GCN），导致模型难以泛化且计算效率低下。
*   **研究目标**：验证一个简单的、端到端的 Transformer 架构是否能够同时胜任仅姿态、仅轨迹以及两者结合的预测任务，并达到或超越专门化模型的性能。

### 2. 方法论
*   **核心思想**：采用纯粹的 **Transformer Decoder** 架构，将历史观测和未来查询（Queries）视为统一序列，通过自注意力机制同时捕捉空间（姿态内）和时间（序列间）的依赖关系。
*   **关键技术细节**：
    *   **输入处理**：轨迹通过线性层编码，姿态通过两层 MLP 编码。引入**类型嵌入（Type Embedding）**来区分轨迹和姿态 Token，并加入正弦位置编码。
    *   **统一自注意力**：不同于标准的编码器-解码器结构，SimpliHuMoN 将上下文 $C$ 和查询 $Q$ 拼接为 $[C; Q]$，在所有层中进行双向自注意力计算。实验证明这种设计优于传统的交叉注意力。
    *   **多模态预测头**：为了应对运动的不确定性，模型生成 $K$ 个不同的未来假设。
    *   **损失函数**：采用“赢家通吃”（Winner-Takes-All）损失，仅对与真实值最接近的假设进行梯度回传，鼓励模型生成多样化的预测。
*   **模型配置**：提供了“宽型”（Wide: 6层，192维）和“深型”（Deep: 16层，48维）两种变体。

### 3. 实验设计
*   **数据集与场景**：
    *   **姿态预测**：Human3.6M（实验室环境）、AMASS（大规模动捕库）。
    *   **轨迹预测**：ETH-UCY（行人社交场景）、SDD（斯坦福无人机数据集，俯视视角）。
    *   **结合预测**：MOCAP-UMPM（多人交互）、3DPW（真实世界户外场景）。
*   **对比方法**：
    *   **姿态类**：BeLFusion, CoMusion, SkeletonDiff, SLD 等扩散模型。
    *   **轨迹类**：MID, TrajCLIP, NMRF 等。
    *   **结合类**：T2P, EMPMP。
*   **评价指标**：ADE/FDE（平均/最终位移误差）、APE（对齐后的姿态误差）、JPE（关节精度误差）。

### 4. 资源与算力
*   **硬件**：使用了一块 **NVIDIA RTX A6000 GPU**。
*   **训练参数**：所有模型训练 300 个 Epoch，Batch Size 为 64。
*   **效率表现**：在 MOCAP-UMPM 数据集上，SimpliHuMoN 的测试吞吐量是轻量级模型 EMPMP 的 1.8 倍，训练速度也更快。

### 5. 实验数量与充分性
*   **实验规模**：论文在 7 个主流数据集上进行了测试，涵盖了从单人到多人、从实验室到户外的多种场景。
*   **消融实验**：
    *   对比了不同深度与宽度的组合。
    *   验证了多模态预测（$K=6$ vs $K=1$）的必要性。
    *   对比了统一自注意力与交叉注意力的效果。
    *   验证了联合训练（姿态+轨迹）对单一任务性能的提升（互惠性）。
*   **公平性**：严格遵循前序工作的评估协议和数据分割，对比了最新的 SOTA 模型（包括 2024/2025 年的论文），实验设计客观且充分。

### 6. 主要结论与发现
*   **大道至简**：一个精简的 Transformer 架构在所有任务中均达到或刷新了 SOTA 记录，证明了复杂的领域特定设计（如 GCN）并非必须。
*   **互惠效应**：联合建模轨迹和姿态能显著提升各自的预测精度（姿态精度提升约 11%，轨迹精度提升约 13%）。
*   **架构偏好**：实验发现“宽型”模型更擅长捕捉精细的姿态细节，而“深型”模型在处理长程轨迹相干性上表现更好。

### 7. 优点
*   **通用性极强**：无需修改架构即可适配多种任务，甚至可以进行跨数据集的联合训练。
*   **计算高效**：相比于需要多次迭代推理的扩散模型（Diffusion Models），该模型通过单次前向传播即可生成预测。
*   **性能卓越**：在长程预测（FDE 指标）上表现尤为突出，且能生成物理上合理、多样化的运动路径。

### 8. 不足与局限
*   **社交交互建模缺失**：模型目前将场景中的多个人员视为独立个体处理，缺乏显式的社交交互模块（如处理避障、协同动作）。
*   **复杂交互失败**：在处理强耦合动作（如两人手拉手旋转）或极端突发加速度时，模型预测会出现偏差。
*   **联合训练挑战**：虽然可以进行全数据集联合训练，但由于不同数据集规模差异巨大（如 AMASS 远大于 SDD），在不平衡数据上的泛化仍存在性能折损。

（完）
