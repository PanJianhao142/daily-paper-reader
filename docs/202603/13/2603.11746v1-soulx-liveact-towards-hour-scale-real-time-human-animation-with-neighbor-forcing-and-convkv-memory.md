---
title: "SoulX-LiveAct: Towards Hour-Scale Real-Time Human Animation with Neighbor Forcing and ConvKV Memory"
title_zh: SoulX-LiveAct：利用邻域强迫与 ConvKV 存储实现小时级实时人物动画
authors: "Dingcheng Zhen, Xu Zheng, Ruixin Zhang, Zhiqi Jiang, Yichao Yan, Ming Tao, Shunshun Yin"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11746v1"
tags: ["keyword:MDM", "query:课题"]
score: 8.0
evidence: 长期人体动画与自回归扩散
tldr: 本研究针对自回归扩散模型在长视频生成中的效率与稳定性挑战，提出了SoulX-LiveAct框架。通过引入Neighbor Forcing策略解决扩散状态不匹配导致的训练不稳定，并利用ConvKV Memory机制压缩历史表征，实现了小时级实时人物动画生成。该方法在保持高画质和口型同步的同时，显著降低了推理成本，支持在两块H100 GPU上实现20 FPS的实时流式推理。
motivation: 现有的自回归扩散模型在处理长视频时，面临训练信号不一致和历史表征无限增长导致的推理效率瓶颈。
method: 提出了Neighbor Forcing策略以确保扩散步长一致性，并设计了ConvKV Memory机制将因果注意力中的键值对压缩为固定长度的结构化表征。
result: 该方法实现了小时级实时人物动画生成，在口型准确度、动画质量和情感表达上达到SOTA水平，并支持20 FPS的实时推理。
conclusion: 通过优化自回归机制和内存管理，该研究成功解决了长视频生成的稳定性与效率难题，为大规模实时人物动画应用奠定了基础。
---

## 摘要
自回归（AR）扩散模型通过结合扩散建模与因果推理，为视频合成等序列生成任务提供了一个极具前景的框架。尽管它们支持流式生成，但现有的 AR 扩散方法难以实现高效扩展。在本文中，我们识别了小时级实时人物动画中的两个关键挑战。首先，大多数强迫策略传播的样本级表示具有不匹配的扩散状态，导致学习信号不一致且收敛不稳定。其次，历史表示呈无界增长且缺乏结构，阻碍了缓存状态的有效重用，并严重限制了推理效率。为了应对这些挑战，我们提出了 Neighbor Forcing，这是一种扩散步长一致的 AR 公式，它将时间上相邻的帧作为相同噪声条件下的潜在邻域进行传播。这种设计提供了一个分布对齐且稳定的学习信号，同时保留了整个 AR 链中的漂移。在此基础上，我们引入了一种结构化的 ConvKV 存储机制，将因果注意力中的键（keys）和值（values）压缩为固定长度的表示，从而实现常数内存推理和真正的无限视频生成，而不依赖于短期运动帧内存。大量实验表明，与现有的 AR 扩散方法相比，我们的方法显著提高了训练收敛性、小时级生成质量和推理效率。在数值上，LiveAct 实现了小时级实时人物动画，并支持在仅两块 NVIDIA H100 或 H200 GPU 上进行 20 FPS 的实时流式推理。定量结果表明，我们的方法在口型同步准确度、人物动画质量和情感表达力方面达到了最先进的性能，且推理成本最低。

## Abstract
Autoregressive (AR) diffusion models offer a promising framework for sequential generation tasks such as video synthesis by combining diffusion modeling with causal inference. Although they support streaming generation, existing AR diffusion methods struggle to scale efficiently. In this paper, we identify two key challenges in hour-scale real-time human animation. First, most forcing strategies propagate sample-level representations with mismatched diffusion states, causing inconsistent learning signals and unstable convergence. Second, historical representations grow unbounded and lack structure, preventing effective reuse of cached states and severely limiting inference efficiency. To address these challenges, we propose Neighbor Forcing, a diffusion-step-consistent AR formulation that propagates temporally adjacent frames as latent neighbors under the same noise condition. This design provides a distribution-aligned and stable learning signal while preserving drifting throughout the AR chain. Building upon this, we introduce a structured ConvKV memory mechanism that compresses the keys and values in causal attention into a fixed-length representation, enabling constant-memory inference and truly infinite video generation without relying on short-term motion-frame memory. Extensive experiments demonstrate that our approach significantly improves training convergence, hour-scale generation quality, and inference efficiency compared to existing AR diffusion methods. Numerically, LiveAct enables hour-scale real-time human animation and supports 20 FPS real-time streaming inference on as few as two NVIDIA H100 or H200 GPUs. Quantitative results demonstrate that our method attains state-of-the-art performance in lip-sync accuracy, human animation quality, and emotional expressiveness, with the lowest inference cost.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **SoulX-LiveAct** 的系统，旨在解决长达数小时的实时人物动画生成难题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的自回归（AR）扩散模型在生成长视频（小时级）时面临两大瓶颈：
    1.  **训练信号不一致**：传统的强迫策略（如 Teacher Forcing 或 Self Forcing）在传播历史信息时，往往存在扩散步长（Noise Level）不匹配的问题，导致学习信号不稳定，难以收敛。
    2.  **内存爆炸与效率低下**：随着视频长度增加，KV Cache（键值缓存）呈线性增长，导致内存溢出，且无法有效重用历史状态，限制了推理速度。
*   **研究背景**：人物动画（如数字人直播）需要极高的实时性（20 FPS+）和长时稳定性（数小时不崩坏），而目前的非自回归模型受限于固定长度，自回归模型则受限于效率。

### 2. 方法论：核心思想与关键技术
*   **Neighbor Forcing（邻域强迫）**：
    *   **核心思想**：在自回归链中传播“步长一致”的邻近帧。即在训练时，当前帧和作为参考的邻近帧处于相同的噪声水平（Diffusion Step）。
    *   **技术细节**：通过理论证明（局部平滑性），相同扩散步长的邻近帧在潜在空间中具有统计一致性。这避免了跨步长对齐的复杂性，使模型能直接利用缓存的 KV 状态。
*   **ConvKV Memory（卷积键值存储）**：
    *   **核心思想**：引入一个轻量级的插件式压缩模块，将无限增长的 KV Cache 压缩为固定长度。
    *   **技术细节**：使用 1D 卷积对长时记忆进行压缩（压缩比 $\lambda=5$），将每 5 帧的 KV 状态压缩为 1 帧。配合 RoPE（旋转位置编码）重置操作，确保位置信息在压缩后依然准确。
*   **两阶段训练流程**：
    *   **第一阶段**：使用 Neighbor Forcing 训练音频、文本（情感/动作）与视频的对齐。
    *   **第二阶段**：结合 ConvKV Memory 进行蒸馏训练（DMD 风格），实现 3 步快速推理。

### 3. 实验设计
*   **数据集**：
    *   **HDTF**：专注于面部动态和口型同步。
    *   **EMTD**：包含全身运动和复杂动作。
*   **Benchmark 与对比方法**：
    *   **对比方法**：OmniAvatar、InfiniteTalk、Live-Avatar 等 SOTA 实时模型。
    *   **评价指标**：Sync-C/D（口型准确度）、FID/FVD（图像/视频质量）、VBench（时间/图像质量评分）、VBench-2.0（人体保真度）。

### 4. 资源与算力
*   **推理算力**：在 **2 块 NVIDIA H100 或 H200 GPU** 上实现了 **20 FPS** 的实时流式推理（分辨率 720×416 或 512×512）。
*   **计算效率**：每帧仅需 **27.2 TFLOPs**，显著低于对比方法（如 Live-Avatar 的 39.1 TFLOPs）。
*   **训练细节**：第一阶段使用 300 小时多模态配对数据；第二阶段进行 400 步蒸馏训练。使用了 FP8 精度、序列并行和算子融合技术。

### 5. 实验数量与充分性
*   **实验规模**：
    *   定量实验涵盖了两个主流数据集，并使用了 7 种以上的专业指标。
    *   消融实验针对 Block Size（块大小）、ConvKV 机制、情感/动作编辑模块分别进行了验证。
*   **充分性评价**：实验设计较为充分，不仅对比了生成质量，还重点对比了推理延迟和显存占用，这对于“实时”主题至关重要。通过长视频一致性分析（可视化对比），直观展示了模型在处理 ID 漂移和细节丢失方面的优势。

### 6. 主要结论与发现
*   **步长一致性是关键**：实验证明，简单的“步长一致”邻域参考比复杂的采样策略更能有效稳定自回归扩散模型的训练。
*   **高效压缩可行**：ConvKV Memory 仅增加 1.9% 的推理开销，却能实现常数级的内存占用，支持无限长度生成。
*   **性能领先**：SoulX-LiveAct 在口型同步、人体解剖结构准确性、身份保持（Identity Preservation）方面均达到 SOTA，且推理成本最低。

### 7. 优点与亮点
*   **理论支撑**：为 Neighbor Forcing 提供了数学证明（附录 A），解释了为何相同步长的邻域参考具有更好的归纳偏置。
*   **极高的工程实用性**：针对 H100 硬件进行了深度优化（FP8、算子融合），直接面向工业级直播场景。
*   **长时稳定性**：成功解决了长视频生成中常见的“身份漂移”和“配饰丢失”问题。

### 8. 不足与局限
*   **硬件门槛**：虽然仅需 2 块 GPU，但要求是 H100/H200 等顶级算力卡，对于消费级显卡（如 RTX 4090）的适配性未在文中详细讨论。
*   **压缩损失风险**：虽然 1:5 的压缩比在实验中表现良好，但在极度复杂的背景或剧烈运动下，固定长度的记忆是否会出现信息丢失仍有待极端情况验证。
*   **应用限制**：目前主要聚焦于人物动画，对于更通用的视频生成场景（如复杂物理交互）的泛化能力尚不明确。

（完）
