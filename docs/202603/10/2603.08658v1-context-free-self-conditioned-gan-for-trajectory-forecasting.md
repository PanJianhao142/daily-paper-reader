---
title: Context-free Self-Conditioned GAN for Trajectory Forecasting
title_zh: 用于轨迹预测的无上下文自条件 GAN
authors: "Tiago Rodrigues de Almeida, Eduardo Gutierrez Maestro, Oscar Martinez Mozos"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08658v1"
tags: ["query:课题"]
score: 6.0
evidence: 使用GAN进行人体运动轨迹预测
tldr: 本研究提出一种基于自条件生成对抗网络（Self-Conditioned GAN）的无监督轨迹预测方法，旨在不依赖上下文信息的情况下学习2D轨迹的多模态行为模式。该方法通过在判别器的特征空间中识别不同的运动模式来引导生成过程，并设计了三种训练设置。实验在人体运动和道路代理数据集上展开，结果显示该方法在处理稀有行为模式时表现尤为出色，且在人体运动预测任务中整体优于现有的无上下文方法。
motivation: 旨在通过无监督学习方式从2D轨迹数据中自动挖掘和建模多模态的行为运动模式。
method: 提出一种基于自条件GAN的框架，利用判别器特征空间中的聚类信息作为伪标签来增强生成器的多模态生成能力。
result: 在人体运动和道路代理数据集上，该方法在少数类样本上的表现优于传统无上下文方法，并在人体运动预测中取得了全局领先的性能。
conclusion: 自条件GAN能有效在无监督环境下捕捉轨迹的多样性，为缺乏标注数据的轨迹预测任务提供了一种高效的解决方案。
---

## 摘要
在本文中，我们提出了一种基于自条件生成对抗网络（Self-Conditioned GAN）的无上下文无监督方法，用于从二维轨迹中学习不同的模态。我们的直觉是，在判别器的特征空间中，每种模态都代表一种不同的行为移动模式。我们将该方法应用于轨迹预测问题。我们提出了三种基于自条件 GAN 的不同训练设置，从而产生了性能更优的预测器。我们在两个数据集上测试了我们的方法：人体运动和道路参与者。实验结果表明，我们的方法在代表性最低的有监督标签上优于以往的无上下文方法，同时在其余标签上表现良好。此外，我们的方法在人体运动数据集上实现了全局领先的性能，而在道路参与者数据集上同样表现出色。

## Abstract
In this paper, we present a context-free unsupervised approach based on a self-conditioned GAN to learn different modes from 2D trajectories. Our intuition is that each mode indicates a different behavioral moving pattern in the discriminator's feature space. We apply this approach to the problem of trajectory forecasting. We present three different training settings based on self-conditioned GAN, which produce better forecasters. We test our method in two data sets: human motion and road agents. Experimental results show that our approach outperforms previous context-free methods in the least representative supervised labels while performing well in the remaining labels. In addition, our approach outperforms globally in human motion, while performing well in road agents.