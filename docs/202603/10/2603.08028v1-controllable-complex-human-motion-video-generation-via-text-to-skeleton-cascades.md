---
title: Controllable Complex Human Motion Video Generation via Text-to-Skeleton Cascades
title_zh: 通过文本到骨架级联实现可控的复杂人体动作视频生成
authors: "Ashkan Taghipour, Morteza Ghahremani, Zinuo Li, Hamid Laga, Farid Boussaid, Mohammed Bennamoun"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08028v1"
tags: ["keyword:MDM", "query:课题"]
score: 8.0
evidence: 人体动作扩散中的长程时间依赖性
tldr: 针对复杂人体动作（如空翻、武术）视频生成中，文本控制模糊且骨架序列获取成本高的问题，本文提出一种两阶段级联框架。首先通过自回归模型将文本转为骨架序列，再利用基于DINO-ALF的扩散模型结合参考图像生成视频。此外，还推出了包含2000个复杂动作的合成数据集，在动作多样性和视频一致性上均达到领先水平。
motivation: 现有的视频扩散模型在处理复杂动态动作时，面临文本描述模糊和高质量骨架序列难以获取的双重挑战。
method: 采用两阶段级联框架，先用自回归模型生成文本对应的骨架序列，再通过带有自适应层融合编码器的扩散模型合成最终视频。
result: 在自建合成数据集和Motion-X基准上，该方法在动作多样性、时间一致性和外观保持等方面均优于现有技术。
conclusion: 该研究通过级联生成和高质量合成数据集，有效提升了复杂人体动作视频的可控性和生成质量。
---

## 摘要
生成诸如空翻、侧手翻和武术等复杂人体动作的视频，对于当前的视频扩散模型而言仍具挑战性。仅使用文本作为条件在细粒度动作控制方面存在时间上的歧义，而显式的基于姿态的控制虽然有效，但需要用户提供完整的骨架序列，这对于长且动态的动作来说制作成本很高。我们提出了一个两阶段级联框架来解决这两个局限性。首先，一个自回归文本到骨架模型通过根据先前生成的姿态预测每个关节，从自然语言描述中生成二维姿态序列。该设计捕捉了复杂动作所需的长期时间依赖性和关节间的协调。其次，一个姿态条件的视频扩散模型根据参考图像和生成的骨架序列合成视频。它采用了 DINO-ALF（自适应层融合），这是一种多级参考编码器，能够在剧烈的姿态变化和自遮挡情况下保留外观和服装细节。为了解决复杂人体动作视频生成缺乏公开数据集的问题，我们引入了一个基于 Blender 的合成数据集，包含 2,000 个视频，涵盖了执行杂技和特技动作的多样化角色。该数据集提供了对外观、动作和环境的完全控制。它填补了一项重要空白，因为现有的基准测试在杂技动作方面的代表性严重不足，而从网络收集的数据集则引发了版权和隐私担忧。在我们的合成数据集和 Motion-X Fitness 基准上的实验表明，我们的文本到骨架模型在 FID、R-precision 和动作多样性方面优于之前的方法。我们的姿态到视频模型在 VBench 的时间一致性、动作平滑度和主体保留指标上也达到了所有对比方法中的最佳结果。

## Abstract
Generating videos of complex human motions such as flips, cartwheels, and martial arts remains challenging for current video diffusion models. Text-only conditioning is temporally ambiguous for fine-grained motion control, while explicit pose-based controls, though effective, require users to provide complete skeleton sequences that are costly to produce for long and dynamic actions.   We propose a two-stage cascaded framework that addresses both limitations. First, an autoregressive text-to-skeleton model generates 2D pose sequences from natural language descriptions by predicting each joint conditioned on previously generated poses. This design captures long-range temporal dependencies and inter-joint coordination required for complex motions. Second, a pose-conditioned video diffusion model synthesizes videos from a reference image and the generated skeleton sequence. It employs DINO-ALF (Adaptive Layer Fusion), a multi-level reference encoder that preserves appearance and clothing details under large pose changes and self-occlusions.   To address the lack of publicly available datasets for complex human motion video generation, we introduce a Blender-based synthetic dataset containing 2,000 videos with diverse characters performing acrobatic and stunt-like motions. The dataset provides full control over appearance, motion, and environment. It fills an important gap because existing benchmarks significantly under-represent acrobatic motions while web-collected datasets raise copyright and privacy concerns.   Experiments on our synthetic dataset and the Motion-X Fitness benchmark show that our text-to-skeleton model outperforms prior methods on FID, R-precision, and motion diversity. Our pose-to-video model also achieves the best results among all compared methods on VBench metrics for temporal consistency, motion smoothness, and subject preservation.

---

## 论文详细总结（自动生成）

这是一份关于论文《Controllable Complex Human Motion Video Generation via Text-to-Skeleton Cascades》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：当前的视频扩散模型在生成**复杂人体动作**（如空翻、侧手翻、武术等非重复性、高动态动作）时表现不佳，经常出现肢体轨迹不合理、身体形状不一致或外观漂移。
*   **核心痛点**：
    1.  **控制歧义**：仅靠文本描述难以精确控制细粒度的动作时序。
    2.  **输入成本**：现有的姿态控制模型（Pose-conditioned）需要用户提供完整的骨架序列，这对于普通用户而言制作成本极高。
    3.  **外观失效**：传统的 CLIP 编码器提供的是全局语义特征，在剧烈形变和自遮挡情况下，难以保持人物身份和服装细节（如纹理、局部装饰）。
*   **研究目标**：通过级联架构实现“文本输入 -> 骨架生成 -> 视频合成”，降低控制门槛并提升复杂动作下的视觉稳定性。

### 2. 方法论
论文提出了一个**两阶段级联框架**，核心思想是将“动作规划”与“外观合成”解耦：

*   **第一阶段：文本到骨架生成（Text-to-Skeleton）**
    *   **核心技术**：采用**自回归 Transformer** 模型。
    *   **流程**：将 2D 关节坐标归一化并离散化为 Token（共 $K$ 个 Bin）。按照“帧优先、关节次之”的顺序排列成一维序列。
    *   **逻辑**：模型根据文本提示词（由 CLIP 文本编码器提取特征作为前缀）和已生成的关节 Token，预测下一个关节的位置，从而捕捉长程时间依赖和关节间的空间协调。

*   **第二阶段：姿态驱动的视频生成（Pose-to-Video）**
    *   **核心技术**：基于 **Wan2.1** 视频扩散主干，引入 **DINO-ALF（自适应层融合）**。
    *   **DINO-ALF**：不再依赖单一的 CLIP 图像特征，而是提取 DINOv3 多个 Transformer 层的特征。通过交叉注意力机制，模型学习如何自适应地融合早期层的局部纹理信息和后期层的语义信息，以应对剧烈形变。
    *   **运动编码器**：使用 3D CNN 将渲染后的骨架图编码为时空对齐的 Token，注入到 DiT（Diffusion Transformer）块中。
    *   **训练策略**：使用 LoRA 进行参数高效微调，并引入姿态增强（抖动、丢弃、时移）以增强对第一阶段生成误差的鲁棒性。

### 3. 实验设计
*   **数据集**：
    *   **合成数据集（核心贡献）**：利用 Blender 构建了包含 **2,000 个视频**的复杂动作数据集，涵盖杂技、特技等，解决了真实数据中此类动作稀缺及版权/隐私问题。
    *   **Motion-X Fitness**：用于训练和评估文本到骨架的生成能力。
*   **Benchmark 与对比方法**：
    *   **骨架生成对比**：T2M-GPT, PriorMDM, MLD, HumanDreamer。
    *   **视频生成对比**：HumanVid, MimicMotion, Hyper-Motion, UniAnimate-DiT, VACE。
*   **评估指标**：FID（真实度）、R-precision（语义准确度）、Diversity（多样性）、VBench（涵盖主体一致性、动作平滑度等 7 个维度）。

### 4. 资源与算力
*   **第一阶段（骨架生成）**：使用 **1 张 NVIDIA A100 GPU** 进行训练。
*   **第二阶段（视频生成）**：使用 **4 张 NVIDIA A100 GPU**。
*   **训练细节**：视频模型微调了 40,000 步，采用了 LoRA（Rank=64）策略以节省显存并防止过拟合。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的定量实验（表 I、表 II）和定性视觉对比（图 6、图 7）。
*   **消融实验**：针对 Token 粒度、Transformer 深度、解码策略、DINO-ALF 组件、LoRA 秩、运动编码器架构以及姿态增强策略均做了详细的消融研究（表 III、IV、V、VI）。
*   **客观性**：实验涵盖了从中间骨架质量到最终视频质量的全方位评估，对比了当前最先进的开源和闭源模型思路，实验设计较为客观、公平。

### 6. 主要结论与发现
*   **级联优越性**：先生成骨架再生成视频的路径，比直接从文本生成视频在处理复杂动作时更具物理合理性。
*   **DINO-ALF 的效能**：实验证明 DINO 特征在保持人物身份（如红领带、鞋子颜色）方面远强于 CLIP，尤其是在人物倒立或大幅度旋转时。
*   **合成数据的价值**：高质量的合成数据可以有效训练模型处理极端姿态，且能避免真实视频中的背景噪声干扰。

### 7. 优点
*   **解耦控制**：提供了一个显式且可编辑的中间层（骨架），增强了创作的可控性。
*   **局部特征增强**：DINO-ALF 解决了长久以来 TI2V 模型在复杂运动下“掉装备”（外观漂移）的顽疾。
*   **数据集贡献**：填补了学术界在复杂人体特技动作视频数据集上的空白。

### 8. 不足与局限
*   **单人限制**：目前仅针对单人动作，无法处理多人互动、身体接触或碰撞。
*   **极端细节丢失**：在极高速旋转或远景下，手指、面部等高频细节仍可能出现模糊。
*   **域间差异**：虽然合成数据很有用，但模型在从合成域向真实复杂背景域迁移时，可能仍存在一定的视觉风格偏差。

（完）
