---
title: Towards Scalable Probabilistic Human Motion Prediction with Gaussian Processes for Safe Human-Robot Collaboration
title_zh: 面向安全人机协作的基于高斯过程的可扩展概率人体运动预测
authors: "Jinger Chong, Xiaotong Zhang, Kamal Youcef-Toumi"
date: 2026-03-07
pdf: "https://arxiv.org/pdf/2603.07096v1"
tags: ["query:课题"]
score: 9.0
evidence: 在Human3.6M上的人体运动预测
tldr: 本研究针对人机协作中的安全问题，提出了一种基于结构化多任务变分高斯过程（GP）的人体运动预测框架。该方法利用6D旋转表示确保运动学一致性，并通过关节维度分解实现可扩展性。在Human3.6M数据集上的实验表明，该模型在提供高精度预测的同时，具备优异的不确定性校准能力，且参数量仅为同类深度学习模型的八分之一，非常适合实时机器人部署。
motivation: 为了确保人机协作的安全，机器人需要能够实时预测人类运动并提供可靠且经过校准的预测不确定性估计。
method: 提出一种结合6D旋转表示与关节维度分解的结构化多任务变分高斯过程框架，以捕捉时空相关性并提升计算效率。
result: 该模型在不确定性指标（KDE NLL）上显著优于强基准模型，且在保持竞争力精度的同时，参数量仅为同类方法的八分之一。
conclusion: 实验证明可扩展的高斯过程模型能为机器人运动规划和避障提供准确、可靠且具有可解释性的不确定性预测。
---

## 摘要
具有良好校准不确定性的准确人体运动预测对于安全的人机协作（HRC）至关重要，在这种场景下，机器人必须实时预测并响应人体动作。我们提出了一种用于全身人体运动预测的结构化多任务变分高斯过程（GP）框架，该框架捕捉了时间相关性，并利用关节维度级的分解来实现可扩展性，同时使用连续 6D 旋转表示来保持运动学一致性。在 Human3.6M (H3.6M) 数据集上的评估结果显示，与强基准模型相比，我们的模型在核密度估计负对数似然（KDE NLL）上降低了多达 50，平均连续分级概率评分（CRPS）为 0.021 m，而确定性平均角度误差（MAE）比竞争性的深度学习方法高出 3-18%。经验覆盖率分析表明，预测置信区间内包含的真实结果比例随预测时界的增加而逐渐降低，在低置信区间保持保守，在高置信区间接近名义值，且在较长时界内仅出现适度的校准漂移。尽管采用了概率公式，我们的模型仅需 0.24-0.35 M 个参数，约为同类方法的八分之一，并表现出适中的推理时间，表明其适用于实时部署。广泛的消融实验进一步验证了 6D 旋转表示和 Matern 3/2 + 线性核的选择，并指导了诱导点数量和潜在维度的选择。这些结果表明，可扩展的基于 GP 的模型能够为运动规划和避障等下游机器人任务提供具有竞争力的准确性，以及可靠且可解释的不确定性估计。

## Abstract
Accurate human motion prediction with well-calibrated uncertainty is critical for safe human-robot collaboration (HRC), where robots must anticipate and react to human movements in real time. We propose a structured multitask variational Gaussian Process (GP) framework for full-body human motion prediction that captures temporal correlations and leverages joint-dimension-level factorization for scalability, while using a continuous 6D rotation representation to preserve kinematic consistency. Evaluated on Human3.6M (H3.6M), our model achieves up to 50 lower kernel density estimate negative log-likelihood (KDE NLL) than strong baselines, a mean continuous ranked probability score (CRPS) of 0.021 m, and deterministic mean angle error (MAE) that is 3-18% higher than competitive deep learning methods. Empirical coverage analysis shows that the fraction of ground-truth outcomes contained within predicted confidence intervals gradually decreases with horizon, remaining conservative for lower-confidence intervals and near-nominal for higher-confidence intervals, with only modest calibration drift at longer horizons. Despite its probabilistic formulation, our model requires only 0.24-0.35 M parameters, roughly eight times fewer than comparable approaches, and exhibits modest inference times, indicating suitability for real-time deployment. Extensive ablation studies further validated the choice of 6D rotation representation and Matern 3/2 + Linear kernel, and guided the selection of the number of inducing points and latent dimensionality. These results demonstrate that scalable GP-based models can deliver competitive accuracy together with reliable and interpretable uncertainty estimates for downstream robotics tasks such as motion planning and collision avoidance.

---

## 论文详细总结（自动生成）

这是一份关于论文《Towards Scalable Probabilistic Human Motion Prediction with Gaussian Processes for Safe Human-Robot Collaboration》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：在安全人机协作（HRC）中，机器人不仅需要预测人类的未来动作，还需要知道预测的“信心”程度（即不确定性估计）。
*   **核心挑战**：
    1.  **深度学习局限性**：现有的深度学习模型（如Transformer、扩散模型）虽然精度高，但往往是“黑盒”，缺乏可解释的不确定性校准，且计算资源消耗大，难以实时部署。
    2.  **高斯过程（GP）扩展性难题**：传统GP虽然擅长处理不确定性，但在处理全身人体运动这种高维、大规模数据时，计算复杂度呈立方级增长（$O(N^3)$），难以应用。
*   **研究目标**：开发一种可扩展的、轻量级的概率预测框架，在保证预测精度的同时，提供可靠的不确定性估计。

### 2. 方法论
*   **核心思想**：结合**变分推理**、**多任务学习**与**关节维度分解**，将复杂的全身运动预测任务转化为可并行处理的轻量级高斯过程回归。
*   **关键技术细节**：
    *   **关节维度级分解**：将全身20个关节的运动分解为96个独立的GP模型（每个关节的每个自由度对应一个GP），极大地降低了单模型的输出维度。
    *   **6D旋转表示**：放弃传统的欧拉角或四元数，采用连续的6D旋转表示。这种表示法在数学上更平滑，能更好地满足高斯过程核函数的平滑性假设，避免了旋转空间中的不连续性。
    *   **一发式预测（One-shot Forecasting）**：不同于自回归模型（一步步预测），该模型直接预测未来整个时间序列（F步），有效避免了不确定性在时间步上的递归累积。
    *   **稀疏变分GP（SVGP）**：引入诱导点（Inducing Points）机制，将计算复杂度从 $O(N^3)$ 降低到 $O(NM^2)$（M为诱导点数量），使其能够处理大规模数据集。
    *   **核函数设计**：采用 Matérn 3/2 核（捕捉局部平滑性）与线性核（捕捉长期趋势/漂移）的组合。

### 3. 实验设计
*   **数据集**：使用人体运动预测领域的基准数据集 **Human3.6M (H3.6M)**，包含11名受试者在15种日常活动下的动作捕捉数据。
*   **Benchmark 与对比方法**：
    *   **概率/随机预测基准**：Motron, DLow, ProbHMI, MotionDiff, HumanMAC 等。
    *   **确定性预测基准**：HisRepItself, SiMLPe, QuatNet 等。
*   **评估指标**：
    *   **概率指标**：KDE NLL（核密度估计负对数似然）、CRPS（连续分级概率评分）、经验覆盖率（Coverage）。
    *   **确定性指标**：MAE（平均角度误差）。
    *   **随机指标**：ADE（平均位移误差）、FDE（终点位移误差）、APD（平均成对距离）。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **NVIDIA RTX 3070 Ti** GPU 的工作站上完成。
*   **训练细节**：使用自然梯度下降（NGD）优化变分参数，Adam 优化器优化超参数。
*   **推理效率**：
    *   **参数量**：仅为 **0.24M - 0.35M**，约为竞争对手 Motron 的 1/8。
    *   **推理耗时**：单次预测约 560-685 ms。作者指出，这主要是受限于 GPyTorch 库的并行化实现，单个 GP 的推理仅需 6-7 ms，理论上通过并行化可实现实时运行。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了大规模的**消融实验**，涵盖了旋转表示（6D vs 欧拉角 vs 四元数）、核函数类型、诱导点数量（10到200）、潜在维度（1到6）以及 Batch Size 的对比。
    *   在 H3.6M 的所有动作类别上进行了测试。
*   **充分性评价**：实验设计较为充分且客观。作者不仅对比了精度，还重点分析了**不确定性校准（Calibration）**，通过覆盖率曲线展示了模型在不同置信区间下的表现，这在同类论文中较为少见且具有说服力。

### 6. 主要结论与发现
*   **概率预测领先**：在 KDE NLL 指标上比强基准模型（Motron）提升了多达 50，证明其生成的概率分布更贴合真实分布。
*   **不确定性校准良好**：覆盖率分析显示，模型在短时界内表现保守（有利于安全），长时界内校准漂移较小。
*   **确定性精度尚可**：虽然 MAE 比最先进的深度学习模型高出 3-18%，但在考虑到其极小的参数量和强大的概率建模能力时，这种权衡在机器人任务中是可接受的。
*   **极高的参数效率**：以极小的模型体积实现了 SOTA 级别的概率预测性能。

### 7. 优点
*   **轻量化与可解释性**：相比深度学习，GP 提供了更清晰的数学解释和极小的内存占用。
*   **鲁棒的旋转建模**：通过 6D 旋转表示解决了 GP 在旋转空间建模时的不连续性问题。
*   **开源贡献**：作者发布了 H3.6M 数据预处理流水线和可视化工具，解决了该领域长期存在的数据格式不统一、难以复现的问题。

### 8. 不足与局限
*   **独立性假设**：模型假设关节维度之间是独立的（Factorized），虽然提高了速度，但忽略了人体运动中复杂的跨关节物理耦合（如摆臂与迈步的关联）。
*   **确定性精度差距**：在纯粹的预测准确度（MAE）上仍略逊于专门优化的深度学习架构。
*   **推理延迟瓶颈**：目前的软件实现尚未完全发挥 GP 并行化的潜力，500ms+ 的延迟对于极高频的机器人控制循环仍显略慢。
*   **长时界挑战**：与所有模型一样，在 2 秒以上的长时界预测中，不确定性会显著增大，预测质量下降。

（完）
